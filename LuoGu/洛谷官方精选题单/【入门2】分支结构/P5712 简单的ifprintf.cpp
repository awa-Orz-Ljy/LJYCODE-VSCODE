#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x;
    cin >> x;
    if (x == 0)
        printf("Today, I ate %d apple.", x);
    else if (x == 1)
        printf("Today, I ate %d apple.", x);
    else if (x > 1)
        printf("Today, I ate %d apples.", x);
    return 0;
}